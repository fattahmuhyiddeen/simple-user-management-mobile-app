
const initialState = {
  isLoading: false,
  token: '',
  name: '',
  email: ''
};

export default function reducer(state = initialState, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}
